#Eureka server configurations to register itself


spring.application.name=AuthService
server.port=8082
# DB

admin.username=admin
admin.password=admin
admin.status=Y

jwt.secret=javainuse

eureka:
client:
fetchRegistry: true
serviceUrl:
defaultZone: http://localhost:8761/eureka/



spring.datasource.url=jdbc:mysql://localhost:3306/users?reconnect=true&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC&createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=dSky
spring.datasource.platform=mysql
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.main.web-application-type=reactive
spring.main.allow-bean-definition-overriding=true

#Hibernate

# The SQL dialect makes Hibernate generate better SQL for the chosen databse
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MYSQL5InnoDBDialect

#Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=create-drop

logging.level.org.springframework.transaction.interceptor=TRACE
logging.level.org.springframework.orm.jpa.JpaTransactionManager=DEBUG

logging.level.org.hibernate.SQL=DEBUG
spring.jpa.properties.hibernate.use_sql_comments=true

spring.jpa.open-in-view=true
spring.batch.job.enabled=false